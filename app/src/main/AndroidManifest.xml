<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tk.pankajb.groupix">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home.HomeActivity"></activity>

        <activity android:name=".Credentials.StartActivity" />

        <activity
            android:name=".Credentials.SignInActivity"
            android:parentActivityName=".Credentials.StartActivity" />

        <activity
            android:name=".Credentials.SignUpActivity"
            android:parentActivityName=".Credentials.StartActivity" />

        <activity
            android:name=".EditProfile"
            android:parentActivityName=".Home.HomeActivity" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <activity
            android:name=".Album.AlbumOverview"
            android:parentActivityName=".Home.HomeActivity" />

        <activity
            android:name=".SingleImageView"
            android:theme="@style/AppTheme.SingleImageView" />

    </application>

</manifest>